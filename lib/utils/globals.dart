// ✅ UPDATED GLOBALS.DART - Cart Count Notifier
import 'package:flutter/material.dart';

// Global cart count notifier for real-time cart updates
final ValueNotifier<int> cartCountNotifier = ValueNotifier<int>(0);
